<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
</head>
<body>
    <h2>Edit your message</h2>
    <h3>The Message is Send by <b> <%= chat.from %></b> To <b> <%= chat.to %></b></h3>
    <form method="post" action="/chats/<%= chat._id %>?_method=PUT">
        <textarea name="message" rows="6" cols="30"><%= chat.message %></textarea><br><br>
        <button>Submit</button>
    </form>
</body>
</html>